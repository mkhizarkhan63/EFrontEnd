export enum PmModuleMenu {
    tasks = 'tasks',
    payments = 'payments',
    materials = 'materials',
    updates = 'updates',
}

export enum PmUpdatesMenu {
    all = 'all',
    unread = 'unread',
    flagged = 'flagged',
}

export enum MaterialSubmissionType {
    none = 'none',
    materialQuantitiesRequest = 'materialQuantitiesRequest',
    materialsPurchased = 'materialsPurchased',
    materialsDelivered = 'materialsDelivered',
    confirmMaterialsOnSite = 'confirmMaterialsOnSite',
    materialOptions = 'materialOptions',
    selectOption = 'selectOption',
    subContracted = 'subContracted',
    completedWork = 'completedWork',
}

export enum FileExtensionType {
    none = 'none',
    image = 'image',
    pdf = 'pdf',
}

export enum AffiliationType {
    none = 'none',
    engineer = 'engineer',
    partner = 'partner',
    supervisor = 'supervisor',
    architect = 'architect',
    owner = 'owner',
}

export enum LinkedProfileStatus {
    none = 'none',
    pending = 'pending',
    approved = 'approved',
}

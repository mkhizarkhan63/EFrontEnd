export enum BidStatus {
    none = 'none',
    draft = 'draft',
    continue = 'continue',
    submitted = 'submitted',
    archived = 'archived',
    expired = 'expired',
}

export enum QuantityUnits {
    tons = 'tons',
    m2 = 'm2',
}

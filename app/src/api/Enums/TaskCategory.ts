export enum TaskCategory {
    none = 0,
    followUpTask = 1,
    callContractor = 2,
    visitSite = 3,
    reviewProject = 4,
}

export enum TaskStatus {
    none = 'none',
    upComing = 'upComing',
    due = 'due',
    inDelay = 'inDelay',
    completed = 'completed',
    pending = 'pending',
}

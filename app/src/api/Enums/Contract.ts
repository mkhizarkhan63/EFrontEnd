export enum ContractSubjects {
    none = 'none',
    client = 'client',
    contractor = 'contractor',
    consultant = 'consultant',
}

export enum ContractStatus {
    none = 'none',
    readyToSign = 'readyToSign',
    signed = 'signed',
    liveInPm = 'liveInPm',
    archived = 'archived',
}

export enum ContractStep {
    pricePlan = 'pricePlan',
    details = 'details',
    summary = 'summary',
}

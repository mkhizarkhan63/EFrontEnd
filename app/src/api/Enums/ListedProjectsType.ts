export enum ListedProjectStatus {
    none = 'None',
    draft = 'Draft',
    published = 'Published',
    archived = 'Archived',
}

export enum ListedProjectType {
    none = 0,
    draft = 1,
    published = 2,
    archived = 3,
}

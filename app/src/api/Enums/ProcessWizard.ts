export enum ProcessWizard {
    wait = 'wait',
    inProgress = 'inProgress',
    done = 'done',
    rejected = 'rejected',
}

export enum BidingProcessWizard {
    prices = 'prices',
    plan = 'plan',
}
